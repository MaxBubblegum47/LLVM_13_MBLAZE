# Autogenerated from /home/maxbubblegum/Desktop/LLVM_13_MBLAZE/llvm/utils/lit/tests/lit.site.cfg.in
# Do not edit!

# Allow generated file to be relocatable.
from pathlib import Path
def path(p):
    if not p: return ''
    return str((Path(__file__).parent / p).resolve())


import sys

config.lit_tools_dir = ""
config.llvm_src_root = "/home/maxbubblegum/Desktop/LLVM_13_MBLAZE/llvm"
config.llvm_obj_root = "/home/maxbubblegum/Desktop/LLVM_13_MBLAZE/build"
config.llvm_tools_dir = "/home/maxbubblegum/Desktop/LLVM_13_MBLAZE/build/./bin"

# Support substitution of the tools_dir with user parameters.
# This is used when we can't determine the tool dir at configuration time.
try:
    config.llvm_tools_dir = config.llvm_tools_dir % lit_config.params
except KeyError:
    e = sys.exc_info()[1]
    key, = e.args
    lit_config.fatal("unable to find %r parameter, use '--param=%s=VALUE'" % (key,key))

import lit.llvm
lit.llvm.initialize(lit_config, config)

# Let the main config do the real work.
lit_config.load_config(config, "/home/maxbubblegum/Desktop/LLVM_13_MBLAZE/build/utils/lit/tests/lit.cfg")
